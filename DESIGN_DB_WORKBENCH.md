# DB Workbench Layout Policy

## Scope

PCでのSQL演習を最優先し、モバイル最適化は今回の変更対象外とする。

## Desktop layout

- 問題一覧は最上部に横並びで維持する。
- 下段は左右 **5:5** を基本比率とする。
- 左: 問題文 + DATABASE。
- 右: SQL EDITOR + RESULT。
- DATABASEはデータ行を十分確認できる縦方向の表示面積を維持する。
- SQL EDITORとRESULTは左側と同等の横幅を持たせ、長いSQL・結果列の確認をしやすくする。

## Database inspector

DATABASEは値だけでなく、SQLを書くために必要な意味情報も同じ場所で確認できるようにする。

- 選択テーブルの用途・概要を常時表示する。
- 「定義」表示では既存の列名・型・NULL・既定値に加えて以下を表示する。
  - キー / 制約
  - 備考（列の意味、コード値、単位、参照関係など）
- データ表示でもテーブル概要は残す。
- 実DB定義と矛盾する説明は載せない。DB上でFK制約を設定していない論理参照は、その旨が分かる表現にする。

## Priority

1. PCでの視認性
2. DATABASEとSQL EDITORの往復のしやすさ
3. 列の意味・制約をその場で理解できること
4. モバイル互換性は既存挙動を壊さない範囲のみ維持
