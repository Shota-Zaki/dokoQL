# SQL Reference — Design Policy

## Purpose

演習中にSQL語の意味や基本構文をすぐ確認できる、読み取り専用のSQLリファレンスページを追加する。

## Entry point

- メイン画面のヘッダー右側に `SQL語一覧` リンクを配置する。
- リンクは `target="_blank"` で別タブを開く。
- 演習画面の状態や入力中SQLには影響を与えない。

## Page structure

- ページタイトル: `SQL語の意味と使い方`
- 冒頭に「記述順」と「概念上の処理順」の簡易メモを置く。
- SQL語はカテゴリ別の一覧表で表示する。
- 各行は次の列を持つ。
  - SQL語
  - 意味
  - 基本構文 / 例
  - 主な用途
  - 注意点
- 対象カテゴリ:
  - 検索
  - 条件指定
  - 並べ替え / 件数制御
  - 集計
  - 結合
  - 集合演算
  - 登録 / 更新 / 削除
  - NULL / 条件分岐 / 変換
  - サブクエリ / CTE
  - DDL / 制約
  - 代表関数

## Interaction

- ページ上部にキーワード検索欄を置く。
- 入力した文字列を、SQL語・意味・用途・構文・注意点の全文から部分一致で絞り込む。
- 検索結果0件の場合は明示する。
- 検索はブラウザ内だけで完結し、外部通信しない。

## Visual policy

- 既存の明色・青緑アクセント・高密度な学習UIを継承する。
- SQL語とコード例は等幅フォントを使用する。
- 一覧性を優先し、PCで横幅を十分使う。
- モバイル専用最適化は今回の対象外とする。

## Publication

GitHub Pages公開元の `docs/` にも `index.html`、`sql-reference.html`、`sql-reference.css`、`sql-reference.js` を同期する。
