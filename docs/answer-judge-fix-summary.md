# Answer judge fix summary

The displayed answer example and the SQL used as the judge baseline are now synchronized after detailed content and source-specific overrides are applied. This prevents a copied official answer from being compared against an older internal answer string.
