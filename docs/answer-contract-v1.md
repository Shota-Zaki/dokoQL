# Canonical answer source contract

For each exercise, the SQL shown to the learner, copied by the answer button, and used internally for judging must resolve to the same canonical SQL string after all content integrations and overrides are applied.
