# SQL Practice Lab — Design Policy

## 1. Purpose

ブラウザだけでSQLを入力・実行し、問題演習、DB確認、結果確認、解答例参照、学習進捗保存まで完結できる自己学習用Webアプリとする。

主目的は「問題を読む → テーブルを確認する → SQLを書く → 実行する → 結果を確認する → 次の問題へ進む」を、画面遷移とページ全体のスクロールを極力発生させず最短距離で回すこと。

## 2. UI direction

採用方針: **Desktop-first / single-screen SQL workbench**。

主要領域:
1. 問題
2. DATABASE / テーブル
3. クリック入力
4. SQL EDITOR
5. RESULT

基本配置:

```text
[ compact header: brand | SQL語一覧 | 題材 | 進捗 ]
[ compact selector: 問題 ▼ | Chapter ▼ | 状態 ▼ ]

[ 問題              ][ クリック入力 | SQL EDITOR ]
[ DATABASE / TABLE   ][       RESULT             ]
```

左右の大枠は原則 50% : 50%。右カラム上段は **クリック入力 : SQL EDITOR = 50% : 50%** の縦2列にし、RESULTは右カラム下段で2列分を横断する。

## 3. Single-screen viewport policy

PCではページ全体を原則 `100vh` 内へ収める。

- ページ本体の縦スクロールは原則発生させない。
- ヘッダーと問題選択バーは必要最小限の高さにする。
- 長い問題文、テーブル、スキーマ、実行結果などは各パネル内部でスクロールする。
- クリック入力は例外的に、通常PCでは内部スクロールを発生させないことを優先する。
- 画面高が低いPCでは余白・padding・gapを縮小する。
- モバイルは現在の優先対象外とし、既存挙動を壊さない。

## 4. Problem selector

- 問題一覧はドロップダウンで選択する。
- 問題番号、Chapter名、状態を含める。
- Chapter / 状態フィルター後の問題だけを表示する。
- 現在問題とドロップダウンを同期する。
- `次の問題` は現在のフィルター後一覧に従う。

## 5. Right-column layout

### 上段: クリック入力 + SQL EDITOR

```text
[ クリック入力 50% ][ SQL EDITOR 50% ]
```

### クリック入力

- SQL作成補助ツールとして独立カードにする。
- SQL EDITORとは別枠にする。
- `SQL文・テーブル・カラムを選ぶとカーソル位置へ入ります` の常設説明文は表示しない。
- 通常PCでは、カードの実表示サイズと内容量を測定し、**内部スクロールが発生しない範囲で文字・チップ・操作領域を自動的に最大化**する。
- 固定の極小文字サイズを前提にせず、表示領域に余裕があるほど見出し・カテゴリ・チップを大きくする。
- 自動調整はカード幅・高さ、問題変更、題材変更、テーブル選択、カラム表示変更に追従する。
- どうしても収まらない狭い画面だけ内部スクロールをフォールバックとして許容する。
- チップは文字だけでなく高さと左右paddingも連動して拡大し、押下領域を確保する。
- カテゴリ間には一定の余白と境界を設け、カテゴリ内は詰めすぎない。
- `集計・結合`、`関数・記号`、テーブル名を伴うカラム見出しなど、長いラベルは意味の切れ目で改行して横幅を浪費しない。
- SQL語は句のまとまりが視認できるよう、更新系・並び替え等の開始位置に小さな区切りを設ける。
- **クリック入力は常時展開する。最小化ボタン・展開ボタン・最小化状態の保存は持たない。**
- 過去バージョンで保存された最小化状態が残っている場合も、起動時に解除して常時展開へ移行する。

### SQL EDITOR

- クリック入力と同じ幅の50%列に配置する。
- textareaはカード内の残り高を最大限使用する。
- 実行 / 判定 / DB初期化 / 次の問題 / ヒント / 解答例はエディターカードに残す。

### RESULT

- クリック入力とSQL EDITORの下を横断し、右カラム幅100%を使用する。
- 右カラムの高さの概ね25〜30%程度を目安とする。
- 結果が多い場合はRESULT内部だけをスクロールする。
- `RESULT` と `実行結果` は横並びを維持する。

## 6. Left-column layout

- 上: 問題。必要十分な高さを確保する。
- 下: DATABASE。残り高さを優先する。
- 各カードは `min-height: 0` を許容し、内容量でviewportを押し広げない。

## 7. Database inspector

DATABASEではテーブル一覧、現在データ、列名、型、NULL、既定値、主キー、外部キー、論理参照、CHECK等の制約、用途・備考、コード値・単位・NULL条件を確認できる。

横長・縦長データはDATABASE内部でスクロールする。

## 8. SQL reference

ヘッダーから `SQL語一覧` を別タブで開く。SQL語 / 関数、意味、基本構文・例、主な用途、注意点、SQL記述順、概念上の処理順を検索可能な一覧で提供する。

## 9. Core interactions

- SQLをブラウザ内DBで実行する。
- SELECTは期待結果と比較する。
- INSERT / UPDATE / DELETEは対象テーブル状態を比較する。
- DDLはschema metadataを比較する。
- SQL文字列の完全一致では判定しない。
- DB初期化で問題開始時の状態へ戻す。
- エラー・不正解後も何度でも再試行できる。
- 問題ごとの入力SQL、正誤、解答回数、最終実行日時、見直し、最後の問題、UI状態をlocalStorageへ保存する。

## 10. Technical policy

- 静的ホスティングで動作する。
- ブラウザ内SQL実行環境を利用する。
- 外部サービスへSQLを送信しない。
- UI変更だけで既存SQL実行・判定ロジックを不用意に変更しない。
- rootとGitHub Pages公開用 `docs/` を同期する。

## 11. Accessibility

- キーボードだけで主要操作可能。
- 実行: Ctrl/Cmd + Enter。
- フォーカスリングを消さない。
- クリック入力のチップは判読性と押下領域を両立する。
- 色だけで正誤を伝えない。
- 実行結果テーブルに見出しを付与する。
- 問題ドロップダウンにラベルを付ける。

## 12. Completion criteria

1. PCでページ全体の縦スクロールが原則発生しない。
2. 右上段がクリック入力とSQL EDITORの50:50縦2列になる。
3. クリック入力の常設説明文が表示されない。
4. 通常PCではクリック入力内部のスクロールが発生しない。
5. クリック入力の文字とチップが、収まる範囲で自動的に最大化される。
6. カテゴリの改行・余白・区切りが整理され、SQL語のまとまりが視認しやすい。
7. クリック入力に最小化 / 展開UIがなく、常時展開される。
8. 過去に保存された最小化状態が残っていても常時展開へ移行する。
9. RESULTが右下段を横断する。
10. `RESULT` と `実行結果` が横並びになる。
11. 問題 / DATABASE / クリック入力 / SQL EDITOR / RESULTが1画面内に同時表示される。
12. 既存の実行 / 判定 / DB初期化 / 次の問題が動作する。
13. rootと`docs/`が同期する。
14. 静的サイト検証が成功する。
