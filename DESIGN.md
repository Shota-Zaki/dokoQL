# SQL Practice Lab — Design Policy

## 1. Purpose

ブラウザだけでSQLを入力・実行し、問題演習、DB確認、結果確認、解答例参照、学習進捗保存まで完結できる自己学習用Webアプリとする。

主な学習ループは「問題を読む → テーブルを確認する → SQLを書く → 実行する → 結果を確認する → 次の問題へ進む」。PCでは画面遷移とページ全体のスクロールを極力減らす。

## 2. UI direction

採用方針は Desktop-first / single-screen SQL workbench。

主要領域は、問題、DATABASE、クリック入力、SQL EDITOR、RESULTの5つ。PCでは左右を概ね50:50とし、右上段はクリック入力とSQL EDITORを50:50、RESULTは右下段を横断する。

## 3. Viewport policy

- PCではページ全体を原則100vh内へ収める。
- 長い問題文、テーブル、スキーマ、結果は各パネル内部でスクロールする。
- 各カードは `min-height: 0` を許容し、内容量でviewportを押し広げない。
- モバイルは既存挙動を壊さない範囲で維持する。

## 4. Problem navigation

- 問題一覧はドロップダウンで選択する。
- 問題番号、Chapter名、状態を表示する。
- Chapter / 状態フィルター後の問題だけを表示する。
- `次の問題` は現在のフィルター後一覧に従う。
- 問題切替ではDB初期化、下書き保存など既存処理を再利用する。
- `次の問題` の連打は抑止し、1回の操作につき1回だけ問題切替処理を開始する。

## 5. Exercise text integrity

- 全5題材の原本文は `content/source-prompts-*.js` に保存した転記を正本とする。
- 原本転記は、紙面レイアウト由来の改行・空白だけをWeb表示向けに整え、語句、数値、条件、記号、指定列、並び順など問題の意味を構成する内容は変更しない。
- 問題数は、銀行口座75問、商店70問、RPG70問、正規化22問、設計16問の合計253問とする。
- `exercises.js` および詳細解説データの `prompt` は実行ロジック用の初期値として扱い、起動時に原本転記で置換してからUIへ渡す。
- 判定結果の列名に影響するSELECT問題で解答例にトップレベルの `AS` 句がある場合は、原本文の末尾に `【列別名（AS句）の指定】` として実際に使用すべき `AS xxx` を明示する。
- `AS` 指定以外の使用テーブル、ヒント、説明文、接頭辞・接尾辞を問題文へ連結しない。
- 使用テーブル等の補助情報は `tableGuide` など別メタデータとして保持する。
- 原本文面と既存の解答例で条件・定数・出力列・並び順が食い違う場合は、原本文を改変せず解答例側を原本条件へ合わせる。
- CIで全253問の原本文が正本転記と一致することを検査し、さらに `AS` が必要なSELECT問題では解答例のトップレベル `AS` 句がすべて表示問題文に明記されていることを検査する。

## 6. Dataset fidelity

- 銀行口座・商店・RPGの初期DBは、対象ドリルで想定されている第4版セットアップデータと互換にする。
- テーブル名、列名、列順、データ型、初期行数、初期値をソースセットアップに合わせる。
- ソース側に存在しない `PRIMARY KEY`、`FOREIGN KEY`、`NOT NULL`、`CHECK`、`UNIQUE` 等の制約を独自追加しない。独自制約によってINSERT・重複行・未登録参照を扱う問題の挙動を変えてはならない。
- 意図的な未登録参照、重複コード、廃番済みコード等も演習データの一部として保持し、整合性のために勝手に補正・削除しない。
- 初期データは `content/official-*-seed.sql` のローカル同一オリジンファイルから、アプリ起動時に読み込んでからDBを初期化する。
- `datasets.js` には各ソースのリポジトリ・パス・blob SHAを記録し、どの版に合わせたか追跡可能にする。
- 問題単位の `setupSql` もソースDBの制約条件に合わせる。ソースに存在しないUNIQUE制約を前提とした `ON CONFLICT` 等を使用しない。
- 第三者データの出典・ライセンス条件は `content/THIRD_PARTY_NOTICES.txt` に記録する。
- CIで3題材のテーブル構成、行数、重要な互換行、独自制約の混入を検査する。

## 7. Click input

- SQL作成補助ツールとしてSQL EDITORとは別カードにする。
- SQL文、テーブル、カラムをクリックでカーソル位置へ挿入できる。
- クリック入力は常時展開し、最小化状態を持たない。
- 通常PCでは内容が収まる範囲で文字・チップ・操作領域を自動調整する。
- 自動調整の最小縮小率は `0.68` を下限とし、従来より小さい画面高でも全カテゴリが見切れにくいことを優先する。
- 自動調整は問題変更、題材変更、テーブル選択、カラム表示変更、window resizeなど明示的な契機で行う。
- 初回自動調整は、クリック入力を最終グリッド位置へ移動し `sql-input-palette-card` を付与した後に1回だけ再実行する。最終レイアウト確定前の測定結果をそのまま使わない。
- レイアウト確定後の再計算は明示イベントで起動し、クリック入力のDOM変更を常時監視する `MutationObserver` には依存しない。
- **自分自身のサイズをResizeObserverで監視し、そのコールバック内で自分自身のサイズを変更してはならない。** レイアウト再計算のフィードバックループを禁止する。
- 収まらない場合は縮小を無限に繰り返さず、内部スクロールへフォールバックする。

## 8. SQL EDITOR / RESULT

- textareaはカード内の残り高を最大限使用する。
- 実行 / 判定 / DB初期化 / 次の問題 / ヒント / 解答例を提供する。
- RESULTは右下段を横断し、多い結果はRESULT内部だけをスクロールする。

## 9. Database inspector

DATABASEではテーブル一覧、現在データ、列名、型、NULL、既定値、主キー、外部キー、論理参照、CHECK等の制約、用途・備考を確認できる。

## 10. SQL reference

ヘッダーから `SQL語一覧` を別タブで開く。SQL語 / 関数、意味、基本構文・例、主な用途、注意点、SQL記述順、概念上の処理順を検索可能な一覧で提供する。

## 11. Core interactions

- SQLをブラウザ内DBで実行する。
- SELECTは期待結果、INSERT / UPDATE / DELETEは対象テーブル状態、DDLはschema metadataで判定する。
- SQL文字列の完全一致では判定しない。
- DB初期化で問題開始時の状態へ戻す。
- エラー・不正解後も何度でも再試行できる。
- 問題ごとの入力SQL、正誤、解答回数、最終実行日時、見直し、最後の問題、UI状態をlocalStorageへ保存する。

## 12. Technical / publication policy

- リポジトリ直下のアプリファイルを唯一の実装正本とする。
- 公開コピーを `docs/` 等に二重管理しない。
- GitHub Pages用成果物はCIで `_site/` に毎回クリーン生成し、生成物はGit管理しない。
- 公開対象はビルドスクリプトの明示リストで管理し、README、設計書、検査スクリプト等を公開成果物へ混入させない。
- ブラウザ内SQL実行環境を利用し、入力SQLを外部サービスへ送信しない。

## 13. Accessibility

- キーボードだけで主要操作可能にする。
- 実行は Ctrl/Cmd + Enter。
- フォーカスリングを消さない。
- 色だけで正誤を伝えない。
- クリック入力のチップは判読性と押下領域を両立する。

## 14. Completion criteria

1. PCでページ全体の縦スクロールが原則発生しない。
2. 問題 / DATABASE / クリック入力 / SQL EDITOR / RESULTが同一画面で確認できる。
3. 全253問の原本文が正本転記と一致する。
4. 解答例にトップレベルの `AS` 句があるSELECT問題は、問題文に同じ `AS xxx` がすべて明記される。
5. 使用テーブル等の補助情報を付与しても、原本文と `AS` 指定以外の問題文が変更されない。
6. 問題文と解答例の条件・定数・出力列・並び順に矛盾がない。
7. 銀行口座・商店・RPGの初期DBがソースセットアップと互換で、独自制約による問題実行エラーがない。
8. 問題固有の事前データ投入がソーススキーマ上で実行できる。
9. クリック入力は常時展開され、操作中にブラウザが固まらない。
10. クリック入力の自動調整は `0.68` まで縮小でき、最終レイアウト確定後のサイズを基準に再計算される。
11. ResizeObserver / MutationObserver等による自己リサイズ・自己DOM更新の再帰ループがない。
12. `次の問題` を1回押したときに問題切替が1回だけ実行され、連打で重複処理が開始されない。
13. 実行 / 判定 / DB初期化 / 次の問題が動作する。
14. `npm run check` が成功する。原本文完全一致、AS句明示、初期DB互換チェックを含む。
15. `npm run build:pages` が毎回クリーンな `_site/` を生成する。
16. GitHub Pagesは `_site/` のみを公開する。
