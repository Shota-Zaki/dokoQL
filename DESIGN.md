# SQL Practice Lab — Design Policy

## 1. Purpose

ブラウザだけでSQLを入力・実行し、問題演習、DB確認、結果確認、解答例の参照、学習進捗の保存まで完結できる自己学習用Webアプリとする。

主目的は「問題を読む → テーブルを確認する → SQLを書く → 実行する → 結果を確認する → 次の問題へ進む」という学習ループを、画面遷移を増やさず最短距離で回すこと。

## 2. UI direction

採用方針: **問題演習 + DBワークベンチ型 / Desktop-first**。

### Desktop

縦方向の表示面積は、次の4領域へ最優先で割り当てる。

1. 問題
2. DATABASE / テーブル
3. SQL EDITOR
4. RESULT

それ以外のナビゲーション、題材切替、Chapter、状態、進捗、補助リンクは、可能な限り薄い操作バーへ集約する。

基本配置:

```text
[ compact header: brand | SQL語一覧 | 題材 | 進捗 ]
[ compact selector bar: 問題 ▼ | Chapter ▼ | 状態 ▼ ]

[ 問題                              ][ SQL EDITOR ]
[ DATABASE / TABLE                  ][ RESULT     ]
```

左右は原則 **50% : 50%** とする。

### Problem selector

- 従来の横並び問題カード一覧は廃止する。
- 問題一覧は1つのドロップダウンで選択する。
- 選択肢には最低限 `問題番号 + Chapter名 + 状態` を含める。
- Chapter / 状態フィルターを適用した後の問題だけをドロップダウンへ表示する。
- 現在問題とドロップダウン選択状態は常に同期する。
- `次の問題` 操作は、現在のフィルター後一覧における次の問題へ移動する。

### Compact chrome

4主要領域以外は次の方針で圧縮する。

- ヘッダー高を抑える。
- ブランドマーク、タイトル、進捗表示は1行中心とする。
- ブランド副題はPC作業画面では省略可能。
- SQL語一覧リンク、題材選択、進捗は同じヘッダー行に置く。
- 問題選択、Chapter、状態は1行のセレクターバーにまとめる。
- 問題一覧の専用見出し、検索アイコン、横スクロール一覧は表示しない。
- 問題カード内の公開用補足など、演習に直接不要な常設文言は非表示または最小化する。
- SQLエディタのキーボード説明など補助文言は常時表示しない。
- DATABASEの初期データ説明は常時展開しない。

### Mobile

本プロジェクトの現在の優先対象はPC。既存モバイル挙動は破壊しないが、今回以降のUI判断ではPCの作業効率を優先する。

## 3. Visual language

- 学習用途を優先した高密度UI
- 背景は低彩度の明色
- 強調色は青緑系を中心にする
- 正解 / 完了は緑、誤答 / エラーは赤、見直しは黄
- 大きな装飾画像は使わない
- カードの角丸・影は控えめ
- SQL・DDL・実行結果は等幅フォント
- 操作バーはカードより視覚的に弱くし、主要4領域を目立たせる

## 4. Information architecture

```text
Home
├─ SQL Drill
│  ├─ Bank-like dataset
│  ├─ Store-like dataset
│  └─ RPG-like dataset
├─ Normalization Drill
└─ Database Design Exercise
```

画面上では日本語名称を使用し、内部IDは英数字とする。

## 5. Core learning interactions

### Execute

SQLをブラウザ内DBで実行し、SELECT結果、更新件数、エラーを表示する。

### Judge

1. SELECT: 期待結果と行・列を正規化して比較
2. INSERT / UPDATE / DELETE: 対象テーブル状態を比較
3. DDL: schema metadataを比較
4. 正規化 / 設計問題: 自動判定せず解答例と観点を表示

SQL文字列の完全一致では判定しない。同じ結果を得る妥当な別解を許容する。

### Reset

問題開始時のDB状態へ戻す。

### Next problem

- `実行 / 判定 / DB初期化` と同じ操作列へ配置する。
- 現在のChapter / 状態フィルター後一覧を基準に進む。
- 最終問題では無効化する。
- DB処理中は無効化する。

### Answer example

- 初期状態では閉じる
- ユーザー操作でのみ表示
- 解答SQLまたは設計例
- SQLを構成する考え方
- 実行結果の確認ポイント
- 必要に応じて別解・注意点

## 6. Database inspector

DATABASEは問題文の直下へ配置し、SQLを書く前に必要な情報を確認できることを優先する。

表示対象:

- テーブル一覧
- 現在データ
- 列名
- 型
- NULL可否
- 既定値
- 主キー / 複合主キー
- 外部キー / 論理参照
- CHECK等の制約
- テーブル用途・備考
- 列ごとの意味・コード値・単位・NULL条件等の備考

横長テーブルはDATABASE内部で横スクロールさせ、ページ全体のレイアウトを崩さない。

## 7. SQL reference

ヘッダーから `SQL語一覧` を別タブで開ける。

リファレンスでは次を一覧化する。

- SQL語 / 関数
- 意味
- 基本構文・例
- 主な用途
- 注意点
- SQL記述順
- 概念上の処理順

検索可能とし、演習中に元画面の状態を失わず参照できることを優先する。

## 8. Click-to-insert SQL palette

SQL EDITORではキーボード入力に加えてクリック入力を提供する。

対象:

- SELECT / FROM / WHERE / INSERT / UPDATE / DELETE 等
- AND / OR / LIKE / IN / BETWEEN / GROUP BY / HAVING / JOIN 等
- 演算子・記号
- COUNT / SUM / AVG / MAX / MIN 等
- 現在題材のテーブル名
- 選択テーブルのカラム名

クリック挿入はカーソル位置を尊重し、入力後はSQL EDITORへフォーカスを戻す。

パレットは最小化可能とし、状態をlocalStorageへ保存する。

## 9. Progressive hint policy

SQL問題のヒントは段階的に開示する。

1. 方針
2. 使う句・演算子・関数などの道具
3. SQLの骨組み

正規化・設計問題では、整理の順番 → 関係 → 最終チェックの順で開示する。

問題移動時に開示段階をリセットする。

## 10. Problem table guidance

各問題では対象範囲を常時明示する。

- SELECT: `使用テーブル: ...`
- INSERT / UPDATE / DELETE: `対象テーブル: ...`
- DDL: `作成・変更対象: ...`
- JOIN / 集合演算 / 副問い合わせ: 必要な実テーブルを列挙
- 設計問題: `参照対象: ...`

## 11. Unlimited retry behavior

- SQL実行エラー後も実行 / 判定 / DB初期化を再使用できる。
- エラー後は次の試行へ影響しないDB状態へ復旧する。
- 不正解後も入力SQLを保持する。
- 更新系SQLも毎回問題開始時のDB状態から再試行できる。
- 再試行回数に上限を設けない。
- 正解済み問題も再実行・再判定できる。

## 12. Persistence

ブラウザローカル保存を基本とする。

保存対象:

- 問題ごとの入力SQL
- 正誤
- 解答回数
- 最終実行日時
- 見直しフラグ
- 最後に開いた問題
- UIペイン状態
- クリック入力パレット状態

DBそのものは学習進捗と分離し、問題単位で初期化可能にする。

## 13. Technical policy

- 静的ホスティングで動作すること
- サーバー必須機能をMVPへ入れない
- PostgreSQL互換性の高いブラウザ内SQL実行環境を利用する
- 外部サービスへSQLを送信しない
- DB初期化SQL、問題、解答、判定ロジックを分離する
- rootとGitHub Pages公開用 `docs/` の差分を発生させない
- 既存のSQL実行・判定ロジックをUI変更だけで不用意に変更しない

## 14. Accessibility

- キーボードだけで主要操作可能
- 実行: Ctrl/Cmd + Enter
- フォーカスリングを消さない
- 色だけで正誤を伝えない
- 実行結果テーブルに見出しを付与
- ボタンには動作を示すテキストを付ける
- 問題ドロップダウンには明示的なラベルを付ける

## 15. Completion criteria

1. 問題選択がドロップダウンで行える
2. Chapter / 状態フィルターと問題ドロップダウンが同期する
3. ヘッダー・問題ナビゲーションが主要4領域より明確にコンパクト
4. 問題 / DATABASE / SQL EDITOR / RESULTの表示面積が増える
5. 既存の実行 / 判定 / DB初期化 / 次の問題が動作する
6. SQL語一覧への別タブリンクが維持される
7. GitHub Pages公開用 `docs/` とrootが同期している
8. 静的サイト検証が成功する
