# SQL Practice Lab — Design Policy

## 1. Purpose

ブラウザだけでSQLを入力・実行し、問題演習、DB確認、結果確認、解答例参照、学習進捗保存まで完結できる自己学習用Webアプリとする。

主目的は「問題を読む → テーブルを確認する → SQLを書く → 実行する → 結果を確認する → 次の問題へ進む」を、画面遷移とページスクロールを極力発生させず最短距離で回すこと。

## 2. UI direction

採用方針: **Desktop-first / single-screen SQL workbench**。

主要4領域を最優先する。

1. 問題
2. DATABASE / テーブル
3. SQL EDITOR
4. RESULT

基本配置:

```text
[ compact header: brand | SQL語一覧 | 題材 | 進捗 ]
[ compact selector: 問題 ▼ | Chapter ▼ | 状態 ▼ ]
[ 問題                    ][ クリック入力 ]
[ DATABASE / TABLE        ][ SQL EDITOR   ]
[                         ][ RESULT       ]
```

左右は原則 50% : 50%。

### Single-screen viewport policy

PCではページ全体を原則 `100vh` 内へ収める。

- ページ本体の縦スクロールは原則発生させない。
- ヘッダーと問題選択バーは必要最小限の高さにする。
- 問題 / DATABASE / SQL EDITOR / RESULT は残りの画面高を自動配分する。
- 長いテーブル、スキーマ、実行結果など、内容量が可変なものだけ各パネル内部でスクロール可能にする。
- 問題文が長い場合は問題カード内部でスクロール可能にする。
- 画面高が低いPCでは余白・カードpadding・見出し間隔をさらに縮小する。
- モバイルは現在の優先対象外とし、既存挙動を破壊しない範囲に留める。

### Problem selector

- 問題一覧はドロップダウンで選択する。
- 選択肢には問題番号、Chapter名、状態を含める。
- Chapter / 状態フィルター後の問題だけを表示する。
- 現在問題とドロップダウンを同期する。
- `次の問題` は現在のフィルター後一覧に従う。

### Compact chrome

- ブランド副題はPCでは非表示。
- SQL語一覧、題材、進捗はヘッダー1行へ集約。
- 問題、Chapter、状態は1行のセレクターバーへ集約。
- 問題一覧専用見出し・横カード・検索アイコンは表示しない。
- 公開用補足、キーボード説明、初期データ説明などは常設しない。
- カード間gapと外周paddingは小さくする。

## 3. Right-column layout

右列は次の3段に分け、クリック入力とSQL EDITORを同じ枠へ入れない。

1. **クリック入力**
   - SQL作成補助ツールとして独立したカードにする。
   - SQL EDITORの外側・直上へ配置する。
   - クリック入力内の見出し、補足文、行ラベル、SQLチップはSQL EDITORより小さい文字サイズとする。
   - チップの高さ・左右padding・行間・gapを圧縮し、1行に載る項目数を増やす。
   - カード自体は従来より少し高くし、通常のPC幅・高さでは展開状態でも縦スクロールを極力発生させない。
   - 目安として通常PCでは約180〜210px、高さの低いPCでは約150〜175pxまで確保する。
   - それでも内容が収まらない特殊な画面幅だけ内部スクロールを許容する。
   - 最小化状態は既存のlocalStorage保存を維持する。

2. **SQL EDITOR**
   - 右列で最も大きい領域にする。
   - クリック入力を分離したうえで、textareaへ可能な限り残り高を割り当てる。
   - 実行 / 判定 / DB初期化 / 次の問題 / ヒント / 解答例はエディターカードに残す。

3. **RESULT**
   - SQL EDITORより明確に小さくする。
   - 右列全体の概ね20〜25%程度を上限目安とし、半分未満を維持する。
   - 結果が多い場合はRESULTカード内部だけをスクロールする。
   - 見出しは `RESULT` と `実行結果` を縦積みせず横並びにする。

## 4. Left-column layout

左列:
- 上: 問題。必要十分な高さを確保しつつDATABASEを圧迫しすぎない。
- 下: DATABASE。残り高さを優先して使用する。

各列・各カードは `min-height: 0` を許容し、内容量によってviewportを押し広げない。

## 5. Database inspector

DATABASEでは以下を確認できる。

- テーブル一覧
- 現在データ
- 列名 / 型 / NULL / 既定値
- 主キー / 複合主キー
- 外部キー / 論理参照
- CHECK等の制約
- テーブル用途・備考
- 列ごとの意味・コード値・単位・NULL条件

横長・縦長データはDATABASE内部でスクロールする。

## 6. SQL reference

ヘッダーから `SQL語一覧` を別タブで開く。

- SQL語 / 関数
- 意味
- 基本構文・例
- 主な用途
- 注意点
- SQL記述順
- 概念上の処理順

を検索可能な一覧で提供する。

## 7. Core learning interactions

### Execute
SQLをブラウザ内DBで実行し、SELECT結果、更新件数、エラーを表示する。

### Judge
- SELECT: 期待結果と行・列を比較
- INSERT / UPDATE / DELETE: 対象テーブル状態を比較
- DDL: schema metadataを比較
- 設計問題: 自動判定せず解答例と観点を表示

SQL文字列の完全一致では判定しない。

### Reset
問題開始時のDB状態へ戻す。

### Next problem
- 実行 / 判定 / DB初期化と同じ操作列へ配置。
- 現在のChapter / 状態フィルター後一覧を基準に進む。
- 最終問題・DB処理中は無効化。

## 8. Retry / persistence

- SQL実行エラー・不正解後も何度でも再試行できる。
- 入力SQLを保持する。
- 更新系SQLは問題開始時のDB状態へ戻して再試行できる。
- 問題ごとの入力SQL、正誤、解答回数、最終実行日時、見直し、最後の問題、UI状態をlocalStorageへ保存する。

## 9. Technical policy

- 静的ホスティングで動作する。
- ブラウザ内SQL実行環境を利用する。
- 外部サービスへSQLを送信しない。
- DB初期化SQL、問題、解答、判定ロジックを分離する。
- UI変更だけで既存SQL実行・判定ロジックを不用意に変更しない。
- rootとGitHub Pages公開用 `docs/` を同期する。

## 10. Accessibility

- キーボードだけで主要操作可能。
- 実行: Ctrl/Cmd + Enter。
- フォーカスリングを消さない。
- 色だけで正誤を伝えない。
- 実行結果テーブルに見出しを付与する。
- 問題ドロップダウンにラベルを付ける。

## 11. Completion criteria

1. PCでページ全体の縦スクロールが原則発生しない。
2. クリック入力とSQL EDITORが別カードで表示される。
3. クリック入力内の文字・チップがコンパクトになり、通常PCでは展開状態でも内部縦スクロールが原則発生しない。
4. SQL EDITORが右列で最大の領域になる。
5. RESULTは右列の半分未満で、概ね20〜25%程度に抑える。
6. `RESULT` と `実行結果` が横並びになる。
7. 問題 / DATABASE / SQL EDITOR / RESULTが1画面内に同時表示される。
8. 既存の実行 / 判定 / DB初期化 / 次の問題が動作する。
9. rootと`docs/`が同期する。
10. 静的サイト検証が成功する。
