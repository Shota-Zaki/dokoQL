# SQL Editor Navigation — Design Policy

## Purpose

SQL演習の反復操作で問題一覧へ視線を戻す回数を減らし、実行・判定後に次の問題へ直接進めるようにする。

## Placement

SQL EDITORの操作列は次の順序とする。

`▶ 実行` → `✓ 判定` → `↻ DB初期化` → `次の問題 →`

## Behavior

- `次の問題 →` は現在画面の問題一覧に表示されている項目を基準にする。
- Chapter・状態フィルターで絞り込まれている場合、その絞り込み結果内の次の問題へ進む。
- 問題切替には既存の問題選択処理を再利用し、DB初期化・下書き保存など既存挙動を維持する。
- 現在の問題が絞り込み結果から外れている場合は、表示中の先頭問題へ進む。
- 表示中の問題が1件以下、または末尾問題にいる場合はボタンを無効化する。
- SQL実行・判定中は誤操作防止のため無効化する。

## Scope

PC操作を優先する。モバイル専用最適化は今回の対象外とする。
